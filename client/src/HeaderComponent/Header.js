import React from 'react';

function Header(props) {
    return(
        <header className="header-class">
            <h1>
                Employee Data
            </h1>
        </header>
    );
}

export default Header;
